const {
  addReview,
  getSingleReview,
  getReviews,
  editReview,
  deleteReview,
  getUserReviews,
} = require('./reviewControllers')

module.exports = {
  addReview,
  getSingleReview,
  getReviews,
  editReview,
  deleteReview,
  getUserReviews,
}
