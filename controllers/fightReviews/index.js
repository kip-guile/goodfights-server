const {
  addReview,
  getSingleReview,
  getReviews,
  editReview,
  deleteReview,
} = require('./reviewControllers')

module.exports = {
  addReview,
  getSingleReview,
  getReviews,
  editReview,
  deleteReview,
}
